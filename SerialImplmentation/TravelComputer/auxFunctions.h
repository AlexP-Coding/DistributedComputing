#include <stdio.h>

// Removes and element from an array an returns the new array
int* remove_element(int* arr, int size, int pos);

// Adds an element to an array an returns the new array
short* add_element(short* arr, short size, short element); 